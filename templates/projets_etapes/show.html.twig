{% extends 'base.html.twig' %}

{% block title %}ProjetsEtapes{% endblock %}

{% block body %}
    <h1>ProjetsEtapes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projets_etape.id }}</td>
            </tr>
            <tr>
                <th>IdProjet</th>
                <td>{{ projets_etape.idProjet }}</td>
            </tr>
            <tr>
                <th>IdEtape</th>
                <td>{{ projets_etape.idEtape }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projets_etapes_index') }}">back to list</a>

    <a href="{{ path('projets_etapes_edit', {'id': projets_etape.id}) }}">edit</a>

    {{ include('projets_etapes/_delete_form.html.twig') }}
{% endblock %}
